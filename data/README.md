# Data directories

`raw/olist/` is intentionally gitignored. Place the canonical Olist CSV files there or run `make data` after configuring the Kaggle CLI.

`raw/mock/` is generated by `subscription_commerce.mock_sources` and contains deterministic marketing-spend and catalog-override sources.

`bronze/` is generated by PySpark as Parquet and is also gitignored.
